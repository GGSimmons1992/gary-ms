using System;
using System.Collections.Generic;
using System.Text;

namespace PizzaStore.Tests
{
    public class UserTests
    {
        //Must have an account before ordering
        //Must be able to see their order history
        //Can order up to $5000 worth per order
        //Limit to 1 order per 1 hour
        //Limit to ordering from 1 location “24hrs(i.e.end of business day)”
    }
}
